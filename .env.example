# Generate a long random value before deployment. Never commit the real value.
SECRET_KEY=replace-with-a-random-secret
COOKIE_SECURE=0
RUN_RETENTION_HOURS=24
DEMO_DB_PATH=instance/reconcileflow-demo.db
PORT=8000
